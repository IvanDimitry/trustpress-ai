# TrustPress AI index.html update — Build Trust, Not Vanity

## What this package contains

- `index.html` — updated TrustPress AI page with the new book card added in the same existing style.
- `cover-build-trust-not-vanity.jpg` — new cover image required by the card.
- `new-book-card-only.html` — the standalone card snippet, if you prefer to paste manually.
- `change-summary.json` — quick machine-readable summary.

## Upload instructions

Target URL:

`https://ivandimitry.github.io/trustpress-ai/`

Target repo:

`IvanDimitry/trustpress-ai`

Upload / replace:

1. Replace the existing `index.html` with this new `index.html`.
2. Upload `cover-build-trust-not-vanity.jpg` into the same root folder as `index.html`.

Do not create a new folder.

## What changed

- The page now says 4-book library instead of 3-book library.
- Hero CTA now says “Explore the 4 books.”
- The library intro now says “Four books.”
- Added the new card: Build Trust, Not Vanity.
- Updated JSON-LD structured data to include the new book.
- Updated the old “Third Book” nav label to “New Book.”
- Added a CTA to Build Trust, Not Vanity in the bottom CTA section.

## Test after upload

- Open the live page.
- Hard refresh with Ctrl + F5.
- Check that the new cover loads.
- Click “Open book page” on Build Trust, Not Vanity.
- Test on mobile.
